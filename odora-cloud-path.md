# Odora — مسار السحابة والتحكّم عن بُعد (Cloud / Remote Path)

> # ⛔⛔ للتخطيط فقط — لا يُنفَّذ ⛔⛔
> **إلى Antigravity وأي منفّذ:** **ممنوع بناء أي شيء من هذا الملف** حتى يقول المالك (زكريا) صراحةً **"ابدأ مسار السحابة"**.
> اترك `CloudTransport.ts` مجرّد stub. هذا الملف مرجع مستقبلي، ليس مهمة.
> **آخر تحديث:** 2026-09-08 · مكمّل لـ [odora-implementation-plan.md](odora-implementation-plan.md) و [mostafa-diffuser-app.md](mostafa-diffuser-app.md).

---

## 1) لماذا هذا مؤجّل
- الـ MVP = **تحكّم محلي** (BLE أساسي + WiFi مباشر). يعمل والمستخدم قرب الجهاز — يكفي للانطلاق.
- التحكّم عن بُعد (من أي مكان) = **قيمة إضافية لاحقة**، تُبنى فوق نفس التطبيق **بلا إعادة كتابته** (بفضل طبقة `DeviceController` المجرّدة).
- **البناء الآمن أولاً:** لا نلتزم بتكلفة/تعقيد السحابة قبل أن يثبت المنتج المحلي ويقرّر المالك.

---

## 2) خياران للتحكّم عن بُعد (يُختار وقت التفعيل)

### الخيار A — Cloud-to-Cloud عبر Aroma-Link (يتطلّب جهاز WiFi)
```
تطبيق Odora → سحابتنا (Django/VPS) → Aroma-Link cloud API → جهاز WiFi
```
- **لا gateway، لا firmware.** الجهاز (نسخة WiFi) يدقّ سحابة المصنّع؛ سحابتنا تتكامل مع API المصنّع (مفكوك: `ha_aromalink`).
- **الثمن:** تبعية لسحابة المصنّع (توقّفهم/تغييرهم يؤثّر؛ بياناتنا تمرّ بهم).
- المرجع الكامل: §5.5 في الـ spec.

### الخيار B — Gateway ذاتي (يتطلّب جهاز BLE + صندوق بالموقع)
```
تطبيق Odora → سحابتنا (VPS) → MQTT → Gateway (ESP32) بالموقع → BLE → الجهاز
```
- **ملكية كاملة، لا تبعية للمصنّع.** لكن يتطلّب **firmware ESP32 + هاردوير لكل موقع** (أعلى جهداً وتكلفة).
- المرجع الكامل: §6 في الـ spec.

> القرار بين A و B يعتمد على **نوع الجهاز الذي يشتريه مصطفى** (WiFi → A، BLE فقط → B) وعلى رغبته في الاستقلال عن المصنّع.

---

## 3) الـ VPS (عند التفعيل)
- **الاستضافة:** **VPS من Libyan Spider** (لا Shared — IoT يحتاج اتصالات دائمة/منافذ/عمّال خلفية؛ + دفع محلي بالدينار + بيانات محلية).
- **المكوّنات:** Django REST الموجود + (للخيار B) MQTT broker self-hosted (EMQX/Mosquitto) + Celery (جدولة/مزامنة) + WebSocket للحالة الحيّة.
- **السبب في عدم كفاية Shared:** يمنع عادةً الـ broker والـ WebSocket والعمّال والمنافذ المخصّصة (§6 في الـ spec — NAT والاستضافة).

---

## 4) ماذا يلزم لتفعيله لاحقاً (Checklist مستقبلي)
- [ ] المالك يقول صراحةً: "ابدأ مسار السحابة".
- [ ] حُسم نوع الجهاز (WiFi → A، BLE → B).
- [ ] VPS من Libyan Spider مُهيّأ.
- [ ] (A) تأكيد تكامل Aroma-Link cloud API على حساب/جهاز حقيقي.
- [ ] (B) نموذج Gateway ESP32 يعمل (firmware + MQTT + BLE).
- [ ] تفعيل `CloudTransport` خلف `DeviceController` (بلا لمس الـ UI).
- [ ] multi-tenant + RBAC لو دخل عملاء B2B (§10 في الـ spec).

---

## 5) قاعدة التصميم التي تجعل هذا سهلاً لاحقاً
لأن كل تحكّم يمرّ عبر `DeviceController` المجرّد، **إضافة `CloudTransport` مستقبلاً لا تمسّ الشاشات ولا التدفّقات** — فقط نقل جديد خلف نفس الواجهة. هذا هو المكسب الأساسي من بناء الطبقة المجرّدة من البداية.
